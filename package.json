{
  "dependencies": {
    "express": "^5.1.0"
  },
  "scripts": {
    "start": "ts-node-dev app/src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "ts-node-dev": "^2.0.0"
  }
}
